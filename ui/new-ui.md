# New UI: mockup-to-real transfer checklist

The execution tracker for turning `ui/mockup-v2.html` into the real dashboard. We work through it one item at a time: Hardeep picks the next item by its ID (for example "do S2.3"), it gets built, checked off, and any deviation from the mockup gets noted inline.

Rules of engagement:

- One item per session/PR unless items are explicitly bundled.
- An item is done when it works at `admin.php?page=hyve&new=true`, is i18n-wrapped, has no em/en dashes in copy, and matches the mockup (or the mockup gets updated to match reality, noted here).
- Tags: [PRO] pro tier, [PR-dep] blocked on an open PR merging, [backend] needs new PHP/endpoint work, [decision] needs a call before building.

Legend: `[ ]` todo · `[x]` done · `[~]` in progress

**IA revision 2026-07-09**: the top nav flattened to Dashboard / Knowledge Base / Messages / Settings. The S4 (Chat), S5 (AI), S6 (Integrations) and S7 (Settings) surfaces all live as sidebar panels INSIDE the Settings screen now (`?nav=settings&sub=chat-behavior|chat-appearance|ai-provider|ai-advanced|qdrant|api-access|general`). The item IDs below keep their original numbering; only the placement changed. Settings is reachable without an API key; gated sidebar panels are muted and redirect to Provider & model.

## F. Foundations (shell and plumbing)

- [x] **F1. `?new=true` bootstrap**: in lite `src/backend/index.js`/`App.js`, when the URL has `new=true`, mount the new app from `src/backend/next/` instead of the current one. Old UI untouched otherwise. (Done 2026-07-08: `index.js` renders `next/App` when `new=true`.)
- [x] **F2. `next/` scaffold**: folder structure (`next/App.js`, `next/router.js`, `next/screens/*`, `next/components/*`, `next/data/*`), lint clean, builds with the existing wp-scripts setup. (Done 2026-07-08: `next/App.js` + `next/style.scss` land the scaffold; `router.js`/`screens/`/`components/`/`data/` get created by F3/F6 when their first files exist.)
- [x] **F3. Route registry**: data-driven tree for the 7 screens + sub-panels (Dashboard; KB: all/attention/faq + 5 source drills; Messages: conversations/leads/thread; Chat: behavior/appearance; AI: provider/advanced/tools; Integrations; Settings). Each entry: label, icon, component, capability, pro flag, api-required flag. Designed so Pro can register/override entries (decision 7 in `new-ui-plan.md`). (Done 2026-07-08: `next/router.js`; Pro extends via the `hyve.routes` filter; drill-ins are `hidden` subs, planned panels are `planned` subs. Components attach per screen in S items.)
- [x] **F4. Deep linking + browser history**: screen in the URL via `?nav=`, sub-panel via `&sub=`. `navigate()` writes the URL with `history.pushState` and back/forward restores views (popstate), so every view is linkable and history works both ways. Deliberately NO legacy route-key aliases and NO `window.hyve.view` handling: when PR lite#194 (capabilities/submenus) merges, the router gets adapted to what it actually ships (tracked in W2). (Done 2026-07-08: `parseLocation`/`navigate`/`useRoute` in `next/router.js`; App renders a temporary tab row + subnav until F7/F8.)
- [x] **F5. WP palette + Tailwind tokens**: map the mockup's CSS variables (wp-blue #2271b1, borders, surfaces, ok/warn/bad, 2px radii) into the Tailwind config/theme for `next/`. (Done 2026-07-08: tokens live as `--hyve-*` CSS custom properties scoped under `.hyve-next` in `next/style.scss`; Tailwind arbitrary values can reference them, and the layout kit (F6) consumes them directly. Includes temporary shell chrome that F7 replaces. Also done 2026-07-08: wp-components polish pass, `--wp-components-color-accent`/`--wp-admin-theme-color` overrides swap the Gutenberg indigo for WP admin blue, plus compact 30px control sizing; modals get the same via the `.hyve-next-modal` class since they portal outside the app root.)
- [ ] **F6. Layout kit components** (each small, reusable, mockup-faithful):
  - [x] F6.1 Card (header, actions, form footer; done 2026-07-08: `next/components/Card.js`)
  - [ ] F6.2 PlannedCard (dashed variant)
  - [x] F6.3 Chip (ok/warn/bad/muted with dot; done 2026-07-08: `next/components/Chip.js`; PRO/INCOMING variants when upsell design lands)
  - [x] F6.4 StatCard (label, number, foot, optional meter, planned variant; done 2026-07-08: `next/components/StatCard.js`, meter built in so F6.11 is covered too)
  - [x] F6.5 DataTable (columns, row actions, empty state, load more) replacing PostsTable; also used where FAQ/Sitemap had bespoke tables (Done 2026-07-09: `next/components/DataTable.js`, generic `columns[{key,label,align,render}]` on the `.hyve-next-table` styles; the Messages table can be retrofitted onto it later if worth it)
  - [ ] F6.6 Subnav (subsubsub style, badge support)
  - [x] F6.7 FieldRow (label + description left, control right; stacks on mobile; done 2026-07-08: `next/components/FieldRow.js`)
  - [ ] F6.8 BackRow + drill navigation helper
  - [ ] F6.9 EmptyState (icon, title, text)
  - [ ] F6.10 LockNote (pro lock note) + upsell link helper with UTM campaigns preserved (`appearance-settings`, `suggested-questions-settings`, `system-prompt-settings`, `messages-feature`, `custom-data-feature`, `website-crawling-feature`, `sitemap-crawling-feature`, `document-import-feature`, `faq-feature`, `api-search`, `sidebar-banner` equivalent)
  - [x] F6.11 Meter (usage bar; done 2026-07-08 as part of StatCard, `.hyve-next-stat__meter`)
  - [ ] F6.12 Steps (NUX checklist rows with done/locked states)
  - [ ] F6.13 Modal wrapper (use `@wordpress/components` Modal, styled to mockup)
  - [x] F6.14 Snackbar (`core/notices` snackbar; done 2026-07-08: `next/components/Notices.js` + `next/data/useSaveSettings.js` shared save hook with success/error notices)
- [x] **F7. Header bar**: logo mark (the official plugin icon at `assets/images/icon.png` via `window.hyve.assets.images`), plugin name, real version pill (`window.hyve.version`, newly localized from `HYVE_LITE_VERSION`), API status pill (API connected / API not connected; provider-agnostic so it covers Hyve Connect later), Docs link, Upgrade CTA hidden when `window.hyve.license` exists (UTM campaign `header-upgrade`). (Done 2026-07-08: `next/components/HeaderBar.js`; App now bootstraps settings into the `hyve` store on mount, which starts F9.)
- [x] **F8. Tab nav**: 7 tabs with icons, active state synced with router; when no API key, api-required tabs are muted but Dashboard and AI stay usable, with a replace-redirect to Dashboard if the current screen requires the key (Dashboard then shows only the NUX checklist). No count badge on Messages (dropped). No PRO chips anywhere in nav or subnav: users click through and meet the upsell inside the screen (upsells designed later). Capability filtering stays data-only until PR lite#194 (W2). (Done 2026-07-08: `next/components/TabNav.js`.)
- [x] **F9. Store**: reuse the `hyve` data store (settings, hasAPI, totalChunks, serviceErrors); add route/subroute state for the new router. No second source of truth for settings. (Done incrementally, closed 2026-07-09: the shared store is the only store, extended with `attentionCount` along the way. Deliberate deviation: route state lives in the URL via `next/router.js`, not in the store; the old `route` state stays for the old UI only.)
- [x] **F10. ErrorSection + notices**: port service-error rendering and the `apiFetch` serviceErrors middleware into the new shell. (Done 2026-07-09: the middleware in the shared `src/backend/index.js` already runs for both apps and feeds the shared store, so the port was rendering: `next/components/ServiceErrors.js` shows red-bordered notices above the page head on every screen, old strings reused, `is-bad` notice tone added.)
- [x] **F11. tsdk banner slot** [decision]: where the Themeisle promo banner div lives in the new layout (or whether `?new=true` skips it during the prototype). (DECIDED + done 2026-07-09: same placement as the old UI, top of the content area above service errors and the page heading; `#tsdk_banner` div hidden for `hasPro === 'valid'`, `themeisle:banner:init` dispatched after the settings bootstrap, `tsdk_reposition_notice()` called on mount.)
- [x] **F12. Tracking**: `hyveTrk` events on nav + key actions, matching current feature/featureComponent naming. (Done 2026-07-09: `navigate()` fires `dashboard`/`route`/`screen/sub` on user navigations, replace-redirects excluded; ProviderPanel now fires the old `openai`/`api-key`/`added` on key save. Audit: chart days-filter, qdrant connect/disconnect, delete-thread, kb source drills, add-data and get-started shortcuts were already wired; `import-custom-data` belongs to the pro modal, P2.)

## S1. Dashboard screen

- [x] **S1.1 Status notice**: "Chat is live on all pages" wording driven by real `display_mode` (all/include/exclude/manual variants) + "Manage visibility" deep link to Chat > Visibility. (Done 2026-07-08: `VisibilityNotice` in `next/screens/Dashboard.js`; hidden while the knowledge base is empty since the chat is not live then.)
- [x] **S1.2 NUX setup checklist** (#202): steps from real state (hasAPI, totalChunks > 0, isQdrantActive), steps 2 and 3 locked until step 1, Qdrant framed optional, card disappears when required steps done; strictly state-driven, not dismissible. (Done 2026-07-08: `next/components/SetupChecklist.js` + `next/screens/Dashboard.js`; totalChunks read from `window.hyve.stats` at load, live-wired in W1.)
- [x] **S1.3 Stat cards**: Sessions (`stats.threads`), Messages (`stats.messages`), Knowledge base with meter + free-limit foot (`totalChunks`/`chunksLimit`, plain number when Qdrant active, "Need more storage?" link when > 400 and no Qdrant). (Done 2026-07-08: `StatsGrid` in `next/screens/Dashboard.js`; page-load snapshot from `window.hyve.stats`, live refresh stays W1.)
- [~] **S1.4 Hyve Connect quota card** [decision]: dashed placeholder in production or behind a flag until #164. (Built 2026-07-08 as the dashed planned StatCard; renamed from Hyve Agent per plan decision #9; the production-visibility decision stays open for the W7 flag flip.)
- [x] **S1.5 Chat usage chart**: real `window.hyve.chart` data (messages + sessions series, 7/14/30/90-day selector like today). DECIDED: chart.js (already bundled). (Done 2026-07-08: `next/components/UsageChart.js`, one combined line chart with both series per the mockup, replacing the old UI's two stacked bar charts; empty state note when there is no data yet. Card title is "Usage"; Messages is WP blue with light fill, Sessions is solid amber #dba617, picked from `ui/variations-upsell-chart.html`.)
- [x] **S1.6 Recent conversations**: latest threads with title, message count, relative time; View all → Messages. DECIDED (2026-07-08): free shows 3 + an upsell block the height of the 2 missing rows (only when more conversations exist); Pro shows 5; empty state note when there are none. Uses `GET threads` first page as-is; a light "recent" endpoint stays optional [backend]. (Done 2026-07-08: `RecentConversations` in `next/screens/Dashboard.js`, upsell UTM campaign `messages-feature`. Upsell style: the "blue note" variant pinned to the card bottom, picked from `ui/variations-upsell-chart.html`; no avatars on rows.)
- [x] **S1.7 Get started cards**: three cards with router links + tracking (`get-started-shortcut`, same featureValue ids as the old UI). (Done 2026-07-08 in `next/screens/Dashboard.js`; also added the shared page heading style `.hyve-next-pagehead`, currently used by Dashboard only.)
- [x] **S1.8 No-API dashboard state**: Dashboard stays accessible and shows ONLY the setup checklist (no stats, chart, notices or get-started); api-required tabs muted (replaces the old blur overlay). (Done 2026-07-08 in `next/screens/Dashboard.js`.)

## S2. Knowledge Base screen

- [x] **S2.1 Subnav**: All sources / Needs attention (live count badge) [backend: pending+moderation count] / FAQ [PRO]. (Done 2026-07-09: new `GET {api}/data/counts` endpoint returns `{pending, moderation}`; the sum lives in the store as `attentionCount`, fetched on KB screen mount and refreshed after add/update/override/remove; the amber pill renders in the App subnav on entries flagged `badge: true`.)
- [x] **S2.2 Add a source grid**: 5 cards from a source registry (Pro swaps components, clears PRO chips), drill navigation, tracking per source. (Done 2026-07-09: the registry IS the router's `kb.subs.source-*` entries, extended with `icon`/`description`/`track` fields, so pro swaps entries through the existing `hyve.routes` filter per decision #7; grid in `next/screens/KnowledgeBase.js`, tracking `knowledge-base-source` with the old featureValue ids posts/custom/url/sitemap/documents. Free tier: pro cards drill into a locked view reusing the old intro + "... is a Premium feature" upsell copy and UTM campaigns; pro tier sees a placeholder hint until P2.)
- [x] **S2.3 Indexed content table**: unified list with Source column (Page/Post/URL/Document/Custom), chunk counts, status chips, Remove/Update actions. [backend: aggregate WP data + pro knowledge/links/documents into one listing, or client-side merge; today they are separate endpoints] (Lite part done 2026-07-09: WordPress content via `GET data?status=included` with Title / Source / Chunks / Status / Remove, chunk-count chip in the card head, and Messages-style pagination via the shared `next/components/Pagination.js` (also used by the WordPress drill-in and retrofitted into Messages); `GET data` now returns `total` + `per_page` (via `query_page( $args, 20, true )`, which opts into the found-rows count) plus `type` (the post type's singular label) for the Source column and `chunks` (per-post chunk count) for the Chunks column, via `DB_Table::get_counts_by_post_ids()`: one cached GROUP BY over the chunks table, invalidated with `entries_count` on insert/delete. Chunk rows keep their `post_id` locally even with Qdrant active, and all sources share the ingest pipeline, so the same method serves the pro listings (sitemap, links, documents) later. Pro aggregation done 2026-07-09: the listing is now unified. Lite's `get_data` gained the `hyve_data_query_args` (WP_Query args) and `hyve_data_post` (row payload) filters plus a `hyve_data_deleted` action in `delete_data`; pro (inc/Main.php) uses them to include `hyve_docs` in the union when the new UI sends `type=hyve:all` (plain `any` stays WordPress-only so the OLD dashboard's listings are unaffected), to map `_hyve_type` to Custom Data/URL/Sitemap/Document source labels with a `permanent` flag (also falls back to `_hyve_source` when a link row has no title), and to `wp_delete_post` pro entries after lite's chunk cleanup so one DELETE endpoint serves all rows. The Indexed content card gained a Source dropdown (WP post types + pro sources derived from the router registry when licensed) and a results count; the Remove confirm modal switches to permanent-deletion copy for pro rows. Known limit: search is not wired on this card yet, and URL rows only match by crawled page title, not by address (URL lives in meta).)
- [x] **S2.4 Needs attention**: merged Requires Update (`GET data?status=pending`) + Failed Moderation (`GET data?status=moderation`) with Issue column, per-row Update / Review / Retry, "Update all" [backend: bulk update action or client loop]. (Done 2026-07-09: `AttentionPanel` in `next/screens/KnowledgeBase.js`; both statuses are fetched fully (page loop, 10-page safety cap) and merged into one table since these queues stay small, so no pager. Update and Retry both run `action=update` (Retry re-runs moderation, matching the old Failed Moderation page); Update all is a sequential client loop with progress and one summary snackbar; moderation failures escalate to the S2.5 modal.)
- [x] **S2.5 Moderation review modal**: flagged categories with score bars (12 category labels from `utils.js`), Override action (`onProcessData action=override`). (Done 2026-07-09: `next/components/ModerationModal.js`, shared by the Needs Attention panel and the WordPress drill-in, which now escalates add failures to it instead of the interim snackbar; copy reused from the old modal except the em dash, replaced with a semicolon per the copy rule. The `type` prop is ready for the pro knowledge source.)
- [x] **S2.6 FAQ panel** [PRO]: `GET {api}/faq` top questions with counts, Answer opens add-data modal prefilled, Delete row action; free tier shows chips + lock per upsell decision. (Free tier done 2026-07-09: `FaqPanel` in `next/screens/KnowledgeBase.js`, PRO chip + old intro paragraph + the old dummy questions as a faded non-interactive preview table (`.hyve-next-preview`, reusable for other pro previews) + blue upsell reusing the old title/description, UTM `faq-feature`. Working pro panel done 2026-07-09: pro's `src/next/Faq.js` swaps `kb.subs.faq` through `hyve.routes`; Question/Asked/Actions table matching the free preview, Answer opens the shared AddDataModal prefilled with the question (disabled at the chunk limit) and deletes the question after a successful save like the old panel, Delete asks for confirmation first (deviation from the old instant delete, per the destructive-actions rule); flat list without pagination, like the old panel.)
- [x] **S2.7 WordPress drill-in**: content-type select (from `window.hyve.postTypes`), debounced search, add table (`GET data?type&search&offset`), Add action with chunk limit gating, restricted-content confirm modal for private/password posts, "Added" state for included rows. (Done 2026-07-09 in `next/screens/KnowledgeBase.js`: 500ms debounce with a stale-response guard, Type + Visibility columns, Added chip, restricted modal copy reused verbatim, results count in the toolbar. Moderation-failure escalation to the review modal arrives with S2.5; until then `onProcessData` surfaces the failure as an error snackbar. Also added 2026-07-09, beyond the mockup: bulk add via row checkboxes + select-all-on-page, a selection bar with "Add N to Knowledge Base" (the panel's single primary button), a sequential client-side queue over `POST data` with progress and one summary snackbar, a combined restricted-content confirm with Cancel / Skip them / Add anyway, and failed items staying selected for retry. Selection persists across pages/filters; DataTable gained `renderHeader` + a `check` column align for this.)
- [x] **S2.8 Custom data drill-in** [PRO]: search + list (`GET {api}/knowledge`), Add data + Edit via modal, delete inside modal, limit gating. (Free-tier locked drill shipped 2026-07-09 with S2.2; extended 2026-07-09 with the old dummy rows as a faded `.hyve-next-preview` table, matching the FAQ free treatment; generalized the same day to ALL locked sources via a `LOCKED_PREVIEWS` map (URL: example.com pages + Delete; Sitemap: sitemap.xml rows with Queued/Completed chips + Details; Documents: dummy PDFs + Delete), mirroring the old free pages. Working pro panel done 2026-07-09: pro's `src/next/CustomData.js` swaps `kb.subs['source-custom']` through `hyve.routes`; old intro copy, search with 500ms debounce + stale-response guard, numbered pagination (the knowledge endpoint now returns `total`/`per_page` via `query_page( $args, 20, true )`, old load-more clients unaffected), results count, ChunkLimitNotice + Add Data disabled at the limit, Edit through the shared modal, attention badge refreshed after saves.)
- [x] **S2.9 Add/edit data modal**: title + content (4,000 char limit), Save/Delete, moderation-failure escalation to S2.5. Shared by S2.6/S2.8. (Done 2026-07-09: pro's `src/next/AddDataModal.js`; title + content with a live character count against the 4,000 limit, Add/Save via `POST {api}/knowledge[/{id}]`, Delete behind a medium confirm modal, moderation failures escalate to the kit ModerationModal with override; old strings and the `import-custom-data` tracking event reused.)
- [x] **S2.10 Website URL drill-in** [PRO]: URL validation + Crawl (`POST {api}/links`), table with Indexed/Crawling status, Update/Delete per row. (Free-tier locked drill shipped 2026-07-09 with S2.2. Working pro panel done 2026-07-09: pro's `src/next/WebsiteUrl.js` swaps `kb.subs['source-url']` through `hyve.routes`; URL input + Crawl URL (validated, disabled at the chunk limit), numbered pagination (links endpoint now returns `total`/`per_page` like knowledge), Update re-crawls per row, Delete behind a confirm modal. Deviations from the item text: no Indexed/Crawling status column because the crawl is synchronous and `get_links` has no status field (everything listed IS indexed); moderation failures surface as an error snackbar like the old panel since links have no override endpoint.)
- [x] **S2.11 Sitemap drill-in** [PRO]: sitemap table (`GET {api}/sitemap`), Add sitemap modal (fetch `GET sitemap/fetch?url=`, link checkboxes, select/deselect all, import `POST sitemap`), Details modal (indexed links, delete sitemap). (Free-tier locked drill shipped 2026-07-09 with S2.2. Working pro panel done 2026-07-09: pro's `src/next/Sitemap.js` swaps `kb.subs['source-sitemap']` through `hyve.routes`. List with status + Details; imports run in the background (10 pages per pass), so the panel re-polls every 10s while a sitemap is queued (same pattern as the Qdrant migration). Extended 2026-07-09 per feedback (design settled after two iterations): the LIST keeps the plain Queued/Completed chips; the live progress (meter + 'X of Y selected pages imported') shows in the Details modal under the URL, updating from the 10s poll while the modal is open (`Sitemap::add()` stores `total`, the list endpoint adds a fresh `imported` count per sitemap); records created before totals were stored show no meter. Pro-plugin bugs found while testing (all predate the new UI): (1) FIXED 2026-07-10: `Page_Importer::ingest()` now refreshes an existing `_hyve_source` document instead of duplicating it, so concurrent import runners cannot create duplicate pages; (2) FIXED 2026-07-09: `delete_sitemap()` clears the cached pages list per pass (the details view used to show deleted pages), and `Sitemap::add()` refuses a URL whose previous deletion is still draining (delete/re-add race used to eat freshly imported pages); (3) FIXED 2026-07-10 (queue persistence): the remaining links now live in the sitemap record instead of the cron event args, so a fatal/timeout mid-pass loses at most the page being imported; the status poll self-heals stalled queues by rescheduling the event (`get_sitemap` list mode); a 5-minute transient lock ensures only one pass runs at a time (without it the self-heal spawned concurrent passes that double-counted failures: the 35+7=42 incident), skipped lists are deduped, and on completion any skipped entry that actually got imported is dropped (imported wins); passes cap ATTEMPTS at 10 (not just successes) so web-triggered runs stay inside timeouts even when links fail; a link is given up after three failed passes and recorded as skipped (also when the chunk limit halts the import) with a 'N pages could not be imported.' hint in the Details modal; the endpoint strips the internal queue from responses. Legacy queued events created before the change still work (process() reads the event args when the record has no links). Also fixed 2026-07-10: PHP 8.2 `mb_convert_encoding` HTML-ENTITIES deprecation in `Scraper.php`, replaced with `mb_encode_numericentity`; and lite's `update_meta` no longer sets `_hyve_needs_update` on non-viewable post types (pipeline-owned `hyve_docs` entries were flagged on every re-ingest and the update cron, which queries `post_type => any`, could never clear them; 10 stale flags cleaned from the test site). Add New Sitemap modal: URL, Fetch, checkbox link list (`.hyve-next-linklist`, scrollable, added to the lite kit styles) with Select/Deselect All, Proceed; old strings + `import-sitemap` tracking. Details modal (renamed from the old 'Edit Sitemap' since nothing is editable): URL, status chip, live progress meter with skipped-pages note, and (since 2026-07-10) an Indexed-content-style table of every page with Indexed/Failed chips; page deletions decrement the record's `total` (from the modal via `delete_pages` and from the unified list via `hyve_data_deleted`), keeping the progress denominator equal to imported + queued + failed; every row carries a checkbox (select-all header) with 'Delete N selected' (batched through `DELETE sitemap/pages`, 20 URLs per request, which handles indexed and skipped pages server-side in one call; single-row deletes use the same endpoint) and 'Retry N selected' (shown when the selection contains failed pages) bulk buttons plus a per-row Retry (`POST sitemap/retry` accepts one `link` or a `links` array and moves them from `skipped` back into the persisted queue); every row has Delete behind a confirm modal: indexed pages go through the unified `DELETE data?id` (pro's `hyve_data_deleted` handler also clears the sitemap pages cache), failed pages are dismissed via `DELETE sitemap/skipped`; the table refreshes as the poll reports new imports, scrolls inside the modal, and has a client-side search (URL or title) with a results count; select-all follows the active filter; Delete behind a confirm modal warning all imported pages are removed permanently.)
- [x] **S2.12 Documents drill-in** [PRO]: MediaUpload multi-select (PDF/DOCX/MD/TXT/CSV), import queue with Queued/Processing/Needs review/Failed/Done statuses, sensitive-data review modal (categories, Cancel/Import anyway with `confirm_sensitive`), Delete per row. (Free-tier locked drill shipped 2026-07-09 with S2.2. Working pro panel done 2026-07-09: pro's `src/next/Documents.js` swaps `kb.subs['source-documents']` through `hyve.routes`, porting the old queue logic onto the kit: Select Files (primary, card actions; `wp_enqueue_media` already runs on the page), sequential per-file import with chip statuses (Queued muted / Processing warn + spinner / Needs review warn / Failed bad / Done ok), sensitive-data review modal on `needs_confirmation` (file, summary, categories, Cancel skips / Import anyway re-sends with `confirm_sensitive`), failures stay listed with the error under the title; indexed rows show Indexed/Indexing failed chips; Delete behind a confirm modal noting the file stays in the Media Library; numbered pagination (documents endpoint now returns `total`/`per_page`).)
- [x] **S2.13 Chunk-limit notice**: single shared component used across all add flows (replaces today's copy-pasted Notices). (Done 2026-07-09: `next/components/ChunkLimitNotice.js`, reads `hasReachedLimit` from the store, old warning string verbatim.)

## S3. Messages screen

- [x] **S3.1 Subnav**: DECIDED (2026-07-08): Leads is removed from the code entirely (like Tools, it lives in the mockup only until #168 ships). With one visible panel left, the App hides single-link subnavs, so Messages renders with no subnav.
- [x] **S3.2 Conversations table**: `GET threads` with pagination; title + snippet (last message, tags stripped) + message count + date + View. Free tier: first page (3) + the blue note upsell (only when more exist); Pro: Previous/Next pagination with "Page X of Y" in the card footer (10/page). No avatars. [backend, done]: `GET threads` now also returns `total` and `per_page` to drive the pager. (Done 2026-07-08: `next/screens/Messages.js`; base `.hyve-next-table` styles land here for F6.5 to build on.)
- [x] **S3.3 Search**: REMOVED from the plan (2026-07-08). No conversations search, also dropped from the mockup.
- [x] **S3.4 Thread drill-in**: full conversation bubbles with timestamps, meta line (date, count, thread id), Delete conversation with snackbar + back to the list (admin-only until PR lite#194, then `hyve_manage_messages`). Router gained an `&item=<id>` param so threads are linkable and Back works. Deep links resolve from an in-memory cache, falling back to a first-page lookup; a "Conversation not found" card covers stale links. [backend, optional]: a single-thread endpoint would make old deep links resolvable. (Done 2026-07-08.)
- [x] **S3.5 Export CSV**: DECIDED (2026-07-08): the button is always visible; without `window.hyve.exportMessagesURL` (free) it is disabled with a lock icon and does nothing, with it (pro) it downloads. Pro wiring of the localized URL into the new UI is part of P2.
- [ ] **S3.6 Messages-only user rendering**: whole app boots into Messages with other tabs absent (depends on PR lite#194's `view`/capability plumbing).

## S4. Chat screen

- [x] **S4.1 Behavior > Conversation card**: `welcome_message`, `default_message` (TextControls), `sound_enabled` toggle with Enabled/Disabled label, Save. Sits below the visibility card. (Done 2026-07-08: `next/screens/Chat.js`; descriptions reuse the old UI strings to keep translations.)
- [x] **S4.2 Behavior > Suggestions card**: 3 `predefined_questions` fields. Pro: editable + Save. Free: PRO chip in the card head, disabled inputs with example placeholders, blue note upsell (UTM `suggested-questions-settings`). (Done 2026-07-08.)
- [x] **S4.3 Follow-up questions toggle**: `follow_up_questions` ToggleControl in the Suggestions card (default on, enforcement fully server-side in pro). Pro: editable; free: disabled under the card's upsell, whose copy now covers follow-ups too. First admin UI for this setting anywhere (the old UI never got one). (Done 2026-07-09 after hyve#259 merged.)
- [x] **S4.4 Source links toggle**: `show_source_link` ToggleControl in the "Trust & sources" card, reusing the old UI's description string. (Done 2026-07-09 after lite#180 merged into the branch.)
- [x] **S4.5 Privacy notice toggle**: `privacy_notice_enabled` ToggleControl in "Trust & sources" with the linked Settings → Privacy description and the compact warning notice when enabled without a privacy page (`window.hyve.hasPrivacyPage`, `privacySettings`); strings reused from the old UI. (Done 2026-07-09 after lite#192 merged into the branch.)
- [ ] **S4.6 Proactive messages + Lead capture planned cards** [decision: production visibility].
- [x] **S4.7 Appearance form**: `chat_position`, `show_timestamp` (free); `chat_name`, `chat_icon` (icon set via the `hyve.appearance.chat-icons` JS filter + MediaUpload custom image), 4 color tiles with ColorPicker popovers + Reset to defaults [PRO]. Free tier: PRO chips on the locked rows, controls visible but disabled, blue note upsell (UTM `appearance-settings`); the icon-set row only renders when pro provides icons. (Done 2026-07-08: `next/screens/ChatAppearance.js`.)
- [x] **S4.8 Live preview**: DECIDED, deviation from the mockup: no static in-card preview. The real widget already floats on the admin page as a preview-flagged test chat (`enqueue_chat_preview`), so the panel shows a notice pointing at it and pushes every edit through `window.hyveApp.applyPreviewAppearance` (position, timestamps, name, colors + dark-color mapping, icon) as you type. (Done 2026-07-08.)
- [x] **S4.9 Visibility card** (top of the Behavior panel, not its own sub-tab): `display_mode` radio cards, `display_rules` URL-rule editor (path + contains/matches, add/remove), own Save; moved off the old Dashboard. The Dashboard "Manage visibility" shortcut deep-links here and now lands on the real card. (Done 2026-07-08. Also done alongside: page headings moved into the App shell, rendered for every screen from the registry's new `description` field, so they sit above the subnav like the mockup's headrow.)

## S5. AI screen

- [ ] **S5.1 Provider selector**: OpenAI active, "Hyve Connect (coming soon)" disabled [decision: production visibility until #164].
- [x] **S5.2 API key field**: `api_key` password input, status chip (Connected/Not connected/Unsaved + "Get an API key" link when empty), Save posts real settings, updates `hasAPI` (unlocks tabs, ticks checklist step 1), surfaces endpoint warnings. (Done 2026-07-08: `next/screens/AI.js` ProviderPanel.)
- [x] **S5.3 Model select** [PR-dep lite#174]: shipped with the CURRENT model list as a SelectControl (gpt-4o-mini recommended first, 4.1 family, 4o, 3.5 legacy); swap in the refreshed list when PR lite#174 merges. (Done 2026-07-08 in `next/screens/AI.js`.)
- [ ] **S5.4 System prompt** [PRO][PR-dep lite#193 + hyve#252]: `system_prompt` textarea in Settings > AI. Per plan decision #8 this is settings-backed: build in lite gated by license (no filter slot), free tier disabled + upsell.
- [x] **S5.5 Advanced panel**: `temperature`, `top_p` (moved per #248), `similarity_score_threshold` (moved from KB Options; same key), Reset to defaults, Save. (Done 2026-07-08: `next/screens/AI.js` AdvancedPanel.)
- [x] **S5.6 Tools panel** (#195): DECIDED, not present in the real dashboard for now (removed from the AI subnav/registry); stays in the mockup only. Revisit when #195 gets built.

## S6. Integrations screen

- [x] **S6.1 Qdrant card, three states**: not connected (key + endpoint + Connect via settings POST, docs link), migrating (warn chip, meter, chunks-moved count from `GET {api}/qdrant` with 10s polling, cleaned up on unmount), connected (ok chip, cluster host shown, Disconnect). Status chip lives in the card head. (Done 2026-07-09: `next/screens/Integrations.js`, wired as Settings > Integrations > Qdrant; API Access is its own sidebar item.)
- [x] **S6.2 Qdrant disconnect confirm modal**: `POST {api}/qdrant` deactivate with Cancel/Disconnect, warning copy reused from the old UI, state returns to not connected. (Done 2026-07-09.)
- [x] **S6.3 API Access card** [PRO] (renamed from Hyve Connect per plan decision #9): free tier DONE 2026-07-09; description + curl request preview (from the old ExternalSearch page, `window.hyve.rest_url`) render on BOTH tiers so the feature reads as real; free adds the PRO chip + blue note upsell (old Access Tokens paragraph, UTM `api-search`). Pro tier DONE 2026-07-09 with P2: the token manager card from pro's `src/next/AccessTokens.js` fills the `api-access-tokens` slot below the description (masked tokens, Show/Copy, Generate, delete confirm modal).
- [x] **S6.4 Webhooks planned card** (#192): mockup-only per the planned-things-don't-ship rule (same as Tools/Leads); enters the code when #192 is real.

## S7. Settings screen

- [x] **S7.1 License card** [PRO]: masked key, valid/expired/invalid states, Activate/Deactivate (`POST {api}/license`), renew/purchase-history links; absent in free tier. Pro-owned endpoint, so this ships with the P1/P2 pro registration (plan decision #8), not in lite. (Done 2026-07-09: pro's `src/next/License.js` registers Settings > Plugin > License via `hyve.routes`, reusing the routes' translated Plugin group label so it lands right under General; not key-gated. Old Advanced.js strings reused; status chip Active/Expired/Not active; Deactivate is destructive with a confirm modal; success reloads the page since license gates flags localized at page load. Also: a license plan pill in lite's HeaderBar next to the version (chosen from mockup variations): green "Pro" with expiration tooltip / amber "Pro · Inactive" / red "Pro · Expired"; rendered only when pro localizes `window.hyve.license`, click navigates to the License panel; the right side keeps only the API status chip.)
- [x] **S7.2 Site integration card**: `post_row_addon_enabled` toggle, `telemetry_enabled` toggle, auto-saving on change like the old OthersSection (no Save button); descriptions reuse the old UI strings. (Done 2026-07-09: `next/screens/SettingsGeneral.js`, wired as the Settings > Plugin > General panel.)

## P. Pro plugin lockstep (Codeinwp/hyve)

- [x] **P1. Extension surface** [decided 2026-07-08, see plan decision #8]: pro extends via `hyve.routes` plus at most one component-slot filter; document the contract. Settings-backed pro features stay in lite behind the license gate and do NOT move to filters (already shipped that way: suggested questions, appearance branding, export button, messages pagination). (Done 2026-07-09. The contract: (1) whole panels swap by attaching `component` to a `hyve.routes` sub entry, honored by the Settings and Knowledge Base panel resolvers; (2) named sections inside lite panels fill through the single `hyve.slot` filter via `next/components/Slot.js`, called as `applyFilters( 'hyve.slot', fallback, name, props )`. Filter names deliberately carry no "next": they survive the flag flip; no clash with the old UI's `hyve.route`/`hyve.data`, which die at W7. Pro registers from `src/next/index.js` in the pro repo, loaded unconditionally by pro's bundle.)
- [~] **P2. Port pro registrations** (pro-only surfaces only): FAQ, Custom/URL/Sitemap/Documents sources, System prompt (if slot-based), License card, Access tokens. Old filters keep working until the old UI is removed. (Access tokens done 2026-07-09: `src/next/AccessTokens.js` in pro fills the `api-access-tokens` slot with a kit-built card: masked tokens with Show/Copy, Generate, delete confirm modal, old strings + tracking events reused; the lite fallback hint shows only when pro's bundle predates the panel. Also confirmed: `exportMessagesURL` and `license` were already localized by pro's `hyve_options_data` filter, so the Messages Export CSV button works on pro with no further wiring, closing S3.5. License card done 2026-07-09, see S7.1. Custom Data + the shared add/edit modal done 2026-07-09, see S2.8/S2.9. FAQ done 2026-07-09, see S2.6. Website URL done 2026-07-09, see S2.10. Documents done 2026-07-09, see S2.12. Sitemap done 2026-07-09, see S2.11. Remaining: only the system prompt, which is PR-blocked (lite#193 + hyve#252), so P2 is complete for everything buildable today.)
- [x] **P3. Shared components bridge**: replace `window.hyveComponents` (PostsTable/PostModal) with the new DataTable/modal exports; keep the old export until pro is migrated. (Done 2026-07-09: `window.hyveComponents.ui` exposes Card, Chip, ChunkLimitNotice, DataTable, FieldRow, ModerationModal, Pagination and `navigate` from the lite bundle; the old PostsTable/PostModal keys stay untouched until W7.)

## W. Wire-up and ship

- [x] **W1. Live-data audit**: nothing is stubbed (every shipped mutation hits its real endpoint), but page-load snapshots need live wiring: `window.hyve.stats`/`chart` on the Dashboard and the checklist's `totalChunks` should refresh after content changes without a reload. (Done 2026-07-10: new lite `GET stats` endpoint returns `hyve_stats` + new `hyve_chart_data` filter output; store gains `stats`/`chart` slices seeded from the boot snapshot, `totalChunks` now seeded from `stats.totalChunks`; Dashboard refetches on mount and dispatches into the store; StatsGrid, UsageCard, checklist gate and SetupChecklist all read store selectors, so KB changes propagate via existing `setTotalChunks` calls and every Dashboard visit pulls fresh threads/messages/chart. Bonus: `hasReachedLimit` now uses live `state.isQdrantActive` instead of the boot value.)
- [ ] **W2. Capability/submenu integration**: align with PR lite#194 (WP submenus per tab, `window.hyve.view` boot, `canManageMessages`).
- [x] **W3. Responsive pass**: tabs scroll, grids stack, tables scroll horizontally, per mockup breakpoints. (Done 2026-07-10: grids/settings/stat cards already collapsed at 980/640; added narrow-width handling for everything else. Header bar: tighter padding at 782, version pill/Docs link/status label hidden at 640 (dot + title attribute remain, label moved into `.hyve-next__status-label` span), bar and tabs pin to `top: 0`/`64px` below 600 where the admin bar scrolls away. Wrap padding 16px at 782. Wrapping added to toolbars (782), card heads, notices, checklist steps (640, with min-width floors on the text so actions drop below), bulk bar, pagination, modal actions and moderation rows (480, label goes full-width). Tables: tighter cell padding at 782, title column capped at 260px at 640; chart 240px tall at 640. Subnav got a row gap for wrapped lines. All kit-level, so pro panels inherit; verified every pro-used class exists in the lite stylesheet. Follow-up: API Access and Provider & model still overflowed because the field grid and the collapsed settings grid used bare `1fr` tracks, which never shrink below content min-width (long select options, curl snippet, token table); both now use `minmax(0, 1fr)`.)
- [x] **W4. Accessibility pass**: focus states, aria on tabs/subnavs/modals/toggles, Escape closes modals, reduced motion. (Done 2026-07-10. Audit found most of it already in place: nav landmarks with aria-label + aria-current, labeled checkboxes/radios incl. select-all and per-row, radiogroup on display mode, sr-only headers for action columns, labeled icon-only buttons, chart canvas role=img, WP Modal focus trap/Escape. Added: shared focus-visible ring (WP blue, high-contrast-safe transparent outline) for tabs, sublinks, side links, plan pill, doclink, CTA, Get started items, source cards, tiles, and radio cards via :has(input:focus-visible); header status label now hides visually-hidden instead of display:none on phones so screen readers keep it; contrast fixes, side nav group headings, conversation timestamps, and bubble times moved from faint (2.4:1) to muted (4.6:1), remaining faint uses are decorative; aria-hidden on all meters (stat card, Qdrant migration, moderation rows, sitemap progress; each has a visible text equivalent) and on the locked-source dummy preview tables so fake rows are not read as content; checklist done state gained sr-only "Done" next to the check icon.)
- [x] **W5. Copy pass**: i18n coverage, no em/en dashes, human tone (README rule). Error/notice strings must be location-neutral: never name a screen/tab in copy served by shared PHP (fixed in `OpenAI::get_error_message_for_code()` 2026-07-08; sweep for others). (Done 2026-07-10: reviewed all 621 strings across both new UIs. Fixed inherited old-UI bugs: URL Crawling upsell described sitemap crawling (old URLCrawler.js copy-paste), telemetry "not shared third-party entities" missing "with", Custom Data upsell "equipping specialized knowledge" missing "it with", AccessTokens "Your application that use this token". Consistency: "chat bot" to "chatbot"; "Knowledge Base" capitalized everywhere it means Hyve's KB; "The entry is deleted permanently" to "removed permanently" matching the pro modal; "Add %s to Knowledge Base" gained "the"; token delete confirm now follows the house pattern ("Delete this token?" + consequence body); "Can not" to "Cannot", dropped a stray exclamation; "Indexed Links" to "Indexed pages" matching the modal's terminology; Custom Data search label said "Search for Posts" while searching entries, now "Search entries". Sentence case on remaining Title Case chrome: Add sitemap, Select/Deselect all, Generate new token, Access tokens, API access, Select files, Add/Edit data, Enter license key, Renew license, Load more, Override moderation, Post type, Search for posts. Verified: no em/en dashes anywhere, no double spaces, PHP-served strings location-neutral. Kept verbatim: license body copy, Purchase History, "Valid - Expires %s", per-source valid-URL phrasings that match their old-UI translations.)
- [x] **W6. Tests**: update e2e specs (dashboard.spec + new flows), unit tests where logic warrants (per test philosophy: our logic, not framework). (Done 2026-07-10. Old-dashboard specs removed: lite `dashboard.spec.js`, `chat-visibility.spec.js`, `knowledge-base-visibility.spec.js` and pro `dashboard.spec.js`; their still-relevant flows were ported into the new specs. New lite e2e (30 tests, all against `?new=true`): `dashboard.spec.js` (shell/header/tabs, live stat cards fed by a mocked `stats` endpoint, chart render + empty placeholder, setup checklist gating, recent conversations, get-started shortcuts, deep links + browser back, reactive service errors), `knowledge-base.spec.js` (sources grid with Pro badges, locked previews + upsells incl. FAQ, indexed content pagination/result count/chunks chip, indexing-failed chips, remove confirm + DELETE capture, the ported restricted-content flows incl. bulk Skip them, chunk-limit lockout, needs-attention queues + moderation modal + badge), `messages.spec.js` (list, thread drill with bubbles/meta, delete confirm, deep-link not-found, CSV lock, free upsell), `settings.spec.js` (side nav groups, the ported visibility flows, locked suggestions, privacy-notice warning, appearance locks + position, provider save + Unsaved chip, advanced reset, general auto-save, Qdrant form, API access upsell). Full lite suite 50/50 locally. Also fixed two pre-existing flakes: `chat.spec.js` double-initialized the widget (dead listeners; now skips manual init when auto-rendered, both repos) and the threads delete mock used route.continue() instead of route.fallback(), starving the list mock. Unit tests: lite `test-stats-endpoint.php` (payload shape + admin-only) and `test-update-meta.php` (edited-flag, pipeline exemption, moderation-flag clearing), 69/69; pro `test-sitemap.php` (persisted queue + scheduling, duplicate/deleting guards, retry single/all/unknown, decrement_total math + legacy records, delete_pages dismiss/delete/total, three-strike skip then completion, 10-attempt pass cap, the lock, imported-beats-skipped reconciliation, chunk-limit skips) and `test-data-filters.php` (hyve: type mapping, row decoration, title fallback, delete side effects; methods called directly since the plugin's Main bails before lite loads in the test bootstrap), 64/64. Local gotcha: `npm run env:test:unit` reinstalls WP in the SAME wp-env database the e2e site uses, wiping settings/theme and leaving unit rows in `wp_hyve`; re-run `wp-env start` (re-seeds settings), reactivate a theme and truncate `wp_hyve` before e2e. CI is unaffected (separate jobs/DBs). Follow-up same day: pro e2e now runs against DEV lite instead of the wordpress.org zip. Pro `.wp-env.override.json` points at the sibling `../hyve-lite` checkout, and `e2e-tests.yml` checks out both repos side by side, using the lite branch that matches the branch under test when it exists, `development` otherwise, and builds lite before wp-env. That unlocked 26 new pro e2e tests (pro suite 35/35): `appearance-preview.spec.js` (typing an assistant name updates the corner preview widget before saving), `license.spec.js` (plan pill opens the panel, Active chip, deactivate confirm), `custom-data.spec.js` (list + live search, add modal with char counter + POST capture, edit prefilled, delete confirm), `faq.spec.js` (list with ask counts, delete confirm removes, Answer prefills the modal and resolves the question), `website-url.spec.js` (list, URL validation, crawl/update/delete captures with confirm), `documents.spec.js` (status chips, delete confirm keeps the Media Library file), `sitemap.spec.js` (list chips, add modal fetch/selection guards/proceed, queued details progress + skipped note + no footer delete, retry capture, search filter, bulk retry/delete with confirm, per-kind page delete copy, completed sitemap delete flow), `indexed-content.spec.js` (unlocked grid, `hyve:all` union + per-source filtering, permanent-entry remove copy). Pro's stale `chat.spec.js` copy was synced from lite (the emoji-launcher selector predates the appearance work), and pro utils gained `hyveRoute`/`effectiveMethod`/`snackbarWithText` helpers.)
- [ ] **W7. Flag flip plan**: `?new=true` becomes default, old app behind `?legacy=true` for one release, then delete old `parts/` + `Sidebar` + dead styles.
- [ ] **W8. Docs**: update `ui/` docs + AGENTS.md/CLAUDE.md pointers to the new structure; close #223 with a summary.

## Backend inventory (referenced above)

| Item | Needed by | Status |
|------|-----------|--------|
| Unified indexed-content listing | S2.3 | new endpoint or client merge [decision] |
| Needs-attention count | S2.1 | new (cheap count query) |
| Bulk "Update all" | S2.4 | new or client loop [decision] |
| Recent conversations (latest 5) | S1.6 | likely covered by `GET threads`, verify |
| Thread search | S3.3 | new [decision] |
| Everything else | all screens | existing endpoints (see `current-ui-lite.md` §5, `current-ui-pro.md` §4) |

## Suggested order for what remains (Hardeep decides, this is just a sane default)

S2 (Knowledge Base, starts with F6.5 DataTable) → S6 (Integrations) → S7 (General) → remaining F items (F9-F12) → P1/P2 → W.

## Remind me at the end of the sprint

- Take a look at the Knowledge Base > WordPress on design issue with the search bar.
- Restrict Pro features to only active licenses.
- https://github.com/Codeinwp/hyve/issues/257
- https://github.com/Codeinwp/hyve-lite/pull/174
- NUX
- Pro QA running on GH